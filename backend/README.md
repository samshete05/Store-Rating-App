# Backend

Express + PostgreSQL API for the store ratings app.

## Install

```bash
cd backend
npm install
```

## Environment

Copy `.env.example` to `.env` and update the values.

## Database

Run `sql/schema.sql` first, then `sql/seed.sql`.

## Start

```bash
npm run dev
```

API runs on `http://localhost:4000` by default.
